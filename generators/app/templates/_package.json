{
  "name": "react-select-<%= name %>",
  "version": "1.0.0",
  "description": "Demo of react-select bug",
  "main": "index.js",
  "scripts": {
    "build": "browserify script.js -o bundle.js -t babelify"
  },
  "license": "MIT",
  "devDependencies": {
    <% if (es2015) { %>"babel-preset-es2015": "^6.6.0",
<% } -%>    "babel-preset-react": "^6.5.0",
    "babelify": "^7.2.0",
    "browserify": "^13.0.0"
  },
  "dependencies": {
    "react": "<%= reactVersion %>",
    "react-dom": "<%= reactVersion %>",
    "react-select": "<%= reactSelectVersion %>"
  }
}
